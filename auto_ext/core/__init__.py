"""Core logic: config, env discovery, template engine, workdir, LVS checks, runner.

Pure-Python modules with no Qt dependency. The GUI layer (``auto_ext.ui``)
and the CLI (``auto_ext.cli``) both build on these modules.
"""
