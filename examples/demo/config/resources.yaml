# Machine resources. Deliberately NOT part of a Recipe: a Recipe has to
# stay portable between an 8-core box and a 64-core one.
# Generated by auto-ext migrate from project.yaml + tasks.yaml.
# Both source files were left untouched. This file is never overwritten by a
# second migration: delete it if you want it regenerated.
# Lines marked NEEDS CONFIRMATION carry a value nobody has checked against a
# real tool yet -- see the migration report and docs/refactor/OFFICE_TODO.md.
schema_version: 1
resource_id: local
# NEEDS CONFIRMATION: Does your site have Hyperscaling licenses? How many
# Turbo cores?
lvs_num_turbo: 2
# NEEDS CONFIRMATION: read back from the runset; nobody has confirmed
# the licence-wait unit or that this site has Hyperscaling licences.
lvs_run_mt: true
# NEEDS CONFIRMATION: Does your site have a Hyperscaling license at all?
lvs_run_hyper: true
# NEEDS CONFIRMATION: Is the license wait time in minutes?
lvs_license_wait_time: 10
# NEEDS CONFIRMATION: Does your QRC support multiple cores, and which section
# spells that?
quantus_cpu_count: 1
reduction_cpu: 1
max_workers: 1
